% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-rladies.R
\name{theme_rladies}
\alias{theme_rladies}
\title{RLadies+ ggplot2 theme}
\usage{
theme_rladies(base_size = 13, mode = c("light", "dark"), grid = TRUE)
}
\arguments{
\item{base_size}{Base font size in points.}

\item{mode}{Either \code{"light"} (charcoal text on transparent background)
or \code{"dark"} (lavender text on transparent background).}

\item{grid}{Logical. Show major gridlines?}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
A clean ggplot2 theme using the RLadies+ brand palette. Supports
light and dark modes with transparent backgrounds for seamless
embedding in branded documents and dashboards.
}
\examples{
library(ggplot2)

ggplot(mtcars, aes(wt, mpg)) +
  geom_point(colour = rladies_cols("purple"), size = 3) +
  labs(title = "Weight vs Fuel Efficiency") +
  theme_rladies()

ggplot(mtcars, aes(wt, mpg)) +
  geom_point(colour = rladies_cols("purple_50"), size = 3) +
  labs(title = "Dark mode") +
  theme_rladies(mode = "dark")
}
